<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/ToolbarStickEarnMovieTheme</item>
    </style>

    <style name="ToolbarStickEarnMovieTheme" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">?colorPrimary</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="CollapsedAppBar" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="titleTextColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>